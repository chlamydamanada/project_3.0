export type blogCreateType = {
  name: string;
  description: string;
  websiteUrl: string;
};

export type blogCreateServiceType = {
  name: string;
  description: string;
  websiteUrl: string;
  createdAt: string;
};
